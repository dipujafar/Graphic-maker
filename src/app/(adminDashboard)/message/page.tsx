import MessageContainer from '@/components/(adminDashboard)/message/MessageContainer';
import React from 'react';

const MessagePage = () => {
    return (
       <MessageContainer></MessageContainer>
    );
};

export default MessagePage;