import RequestContainer from '@/components/(adminDashboard)/request/RequestContainer';
import React from 'react';

const RequestPage = () => {
    return (
        <div>
            <RequestContainer></RequestContainer>
        </div>
    );
};

export default RequestPage;